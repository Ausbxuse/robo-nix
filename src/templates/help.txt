Usage:
  robo shell
  robo run <command> [args...]
  robo search <library>

Project setup:
  .python-version is required.
  pyproject.toml is managed by uv/project policy.
  robo shell creates missing robo runtime files on first use.

Runtime lookup:
  robo search libassimp.so finds Nix package candidates for missing libraries.
